# Runtime dependencies for the inference API only.
# torch and torchvision are deliberately absent: the model is served through
# onnxruntime, which keeps the deployed image roughly 150 MB instead of ~1 GB
# and cuts cold-start time on free CPU hosting.
fastapi==0.139.0
uvicorn[standard]==0.50.2
python-multipart==0.0.32
pydantic==2.13.4
pillow==11.1.0
numpy==2.2.4
onnxruntime==1.28.0
