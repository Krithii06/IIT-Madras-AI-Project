# Runtime dependencies for the serverless functions in api/.
#
# Vercel reads the requirements.txt at the repository root, not this one, so this
# file is here for anyone running the functions locally and to keep the frontend
# directory self-describing. Keep the pins identical to the root file - a mismatch
# would mean local behaviour diverging from deployed behaviour.
#
# onnxruntime must stay at 1.24.3 or later: earlier releases publish no cp314 wheel
# and Vercel selects CPython 3.14 regardless of .python-version.
onnxruntime==1.28.0
numpy==2.2.4
pillow==11.1.0
